<p style="color: green"><%= notice %></p>

<h1>Links</h1>

<div id="links">
  <% @links.each do |link| %>
    <div class="link">
     <%= render partial: 'link', locals: { link: link } %>
    </div>
  <% end %>
</div>

<%= link_to "New link", new_link_path %>
