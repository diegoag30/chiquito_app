<h1>LINK PRIVADO</h1>
<p>Por favor ingrese la contraseña de su link privado</p>
<%=  form_with(model: @link.becomes(Link), url: redirects_path(@link)) do |f| %>
  <div>
    <div class="form-group">
       <%= f.label :password %>
       <%= f.password_field :password %>
    </div>
    <%= f.button 'Enviar', type: :submit, formmethod: :redirect %>
  </div>
<% end %>